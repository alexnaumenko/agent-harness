---
description: Развернуть базу знаний + обвязку агента в текущем проекте (wiki/, docs/, статистика, чек-лист, жизненный цикл)
---

Разверни в текущей папке базу знаний и обвязку агента.

Используй skill `bootstrap-new-project` (из этого плагина): прочитай его `BOOTSTRAP.md` и выполни процедуру по шагам — спроси стек/домен/деплой/базу, скопируй шаблоны, подставь плейсхолдеры, заведи первый session-log, собери сводку статистики.

Не перезаписывай существующие `CLAUDE.md` / `wiki/` / `docs/` без подтверждения.
