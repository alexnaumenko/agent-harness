# Поломки в бою (production)

> Краткий реестр инцидентов. Полный разбор каждой поломки — в `docs/audits/` по шаблону `docs/audits/TEMPLATE.md`.
> Из каждого разбора добавляется правило в `CODE-REVIEW.md`, чтобы ошибка не повторилась.

| дата | что сломалось | серьёзность | длительность | разбор |
|------|---------------|-------------|--------------|--------|
| — | (пока инцидентов нет) | | | |
