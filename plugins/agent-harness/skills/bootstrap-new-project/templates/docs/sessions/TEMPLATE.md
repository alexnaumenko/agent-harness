# Session: YYYY-MM-DD

**Дата:** YYYY-MM-DD
**Бранч:** main
**Цель:** [одна строка — почему мы здесь, из первого сообщения owner'а]
**Тэги:** [csv: тематика и/или блок, например: setup, api, bug-fix, ux]

---

## Связи

(Заполняй только те подсекции, что реально есть. Пустые удаляй.)

**Опирается на:** (build upon — что было фундаментом для сегодня)
- `sessions/YYYY-MM/YYYY-MM-DD.md` §{секция} — [одна строка зачем]
- `wiki/<doc>.md` — [одна строка зачем]

**Развивает / уточняет:** (elaborates — корректировка прежнего без отмены)
- `<doc>` «<пункт>» — [как именно уточнили]

**Отменяет / переопределяет:** (contradicts — обязательно, если реверсим решение)
- `sessions/YYYY-MM/YYYY-MM-DD.md` §{секция} — [что отменили + почему]

**Связано:** (similar/related — упомянутое, но не прямо родительское)
- `docs/decisions/NNNN-*.md` — [контекст]

---

## Контекст
[Что подтолкнуло сессию, в каком состоянии мы были.]

## Что сделано
[Действия, изменения, коммиты.]

## Решения owner'а
[Явные решения, чтобы потом не разбирать «а это где было».]

## Pre-push + деплой (если код)
lint ✓ · types ✓ · tests N passed, cov X% (gate N) · smoke N/N.
Commit `<sha>`, CI run `<id>`: test ✓ / deploy ✓. Health OK.

## Lessons / observations
[Переносимые уроки, формулировки достойные `docs/gems.md` или memory.]

## Открытые вопросы (carry forward)
- Q-A: [...]
- Q-B: [...]

## Итог
[Сводка дня.]

**Closing YYYY-MM-DD — owner-approved.**
