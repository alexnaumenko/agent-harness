<!--
Проектный чек-лист проверки. Проверяющий (роль агента) проходит по каждому
верхнеуровневому пункту этого файла на этапе VERIFY и выдаёт вердикт:
выполнено / не выполнено / не относится к задаче.

Правила:
- Один верхнеуровневый пункт списка (строка с "- ") = одна проверка.
- Пункт, начинающийся со слова "КРИТИЧНО:" — серьёзность «критично» (блокирует выпуск).
  Остальные — серьёзность «важно».
- Список растёт со временем: каждый разбор поломки (docs/audits/) добавляет сюда
  новое правило, чтобы та же ошибка больше не повторялась.
-->

# Чек-лист проверки кода — {{PROJECT_NAME}}

## Безопасность
- КРИТИЧНО: В коде нет паролей, токенов и ключей — только переменные окружения или secret-менеджер.
- КРИТИЧНО: Весь ввод от пользователя/внешних систем проверяется до использования.
- Сообщения об ошибках не раскрывают чувствительные данные.

## Качество кода
- У каждой публичной функции есть короткое описание назначения.
- Функции небольшие (до ~50 строк), файлы сфокусированы (до ~800 строк).
- Нет глубокой вложенности (>4 уровней) — используются ранние выходы.
- Ошибки обрабатываются явно, не «проглатываются» молча.
- Нет отладочных print/console и закомментированного кода.

## Знание о проекте
- При изменении схемы данных / маршрутов / архитектуры обновлён каталог `wiki/` в этом же изменении.
- Значимое решение зафиксировано в `docs/decisions/` (ADR).

## Тесты
- КРИТИЧНО: Новая функциональность покрыта тестами.
- Есть тест, воспроизводящий каждую найденную ранее ошибку (защита от повтора).

## Граблиный список (footguns)
- (пусто — заполняется по мере разборов поломок в docs/audits/)
